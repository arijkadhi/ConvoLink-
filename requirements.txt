# Core Framework (Updated for Python 3.13 compatibility)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
email-validator>=2.0.0

# Database (SQLite - FREE, no additional packages needed)
sqlalchemy>=2.0.35
alembic>=1.13.0

# PostgreSQL (OPTIONAL - only if using PostgreSQL instead of SQLite)
# Uncomment these lines if you need PostgreSQL:
# psycopg2-binary==2.9.9
# asyncpg==0.29.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.1.3
python-multipart>=0.0.9

# Email Service (SendGrid API)
sendgrid>=6.11.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
httpx>=0.27.0

# Environment & Configuration
python-dotenv>=1.0.0

# Logging
python-json-logger>=2.0.7

# Date & Time
python-dateutil>=2.8.2
